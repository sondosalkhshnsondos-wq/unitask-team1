{
  "name": "unitask",
  "version": "1.0.0",
  "description": "UniTask Student Task Manager",
  "scripts": {
    "test": "jest --testPathPattern=test/"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/test/**/*.test.js"]
  }
}
